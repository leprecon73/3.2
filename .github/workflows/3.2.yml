name: Render
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Wait for Deploy
    runs-on: ubuntu-latest
    steps: 
      - name: Wait for Render Deployment
        uses: leprecon73/render-deploy-action
        with:
          render-token: ${{ secrets.RENDER_TOKEN }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          #service-id: srv-cgoo8tou9tun42q1ftig
          #retries: 20
          #wait: 16000
          #sleep: 30004
